import Home from '../../page-objects/home.page';

describe('install', function () {
	it.skip('setup', function () {
		browser.url('/');
	});
	it.skip('login', function () {
		Home.login();
		//TODO 
	});
	it.skip('docker installed', function () {
		
	});
});